{
  "name": "@cok/{{packageName}}",
  "version": "0.0.1",
  "description": "{{description}}",
  "keywords": [
    "{{packageName}}"
  ],
  "main": "src/index.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "directory": "{{outDir}}/{{packageName}}"
  },
  "scripts": {
    "build": "tsup src/index.ts --format=esm,cjs --dts",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "@cok/tsconfig": "workspace:*",
    "rimraf": "^3.0.2",
    "tsup": "^4.14.0",
    "typescript": "^4.2.3"
  }
}